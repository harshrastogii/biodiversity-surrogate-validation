# Python dependencies for the Version 2 analysis pipeline.
# Pinned to the versions used to produce the frozen results (Python 3.13).
# NOTE: reading the NVIS v7 raster geodatabase requires a GDAL build with the
# OpenFileGDB *raster* driver (GDAL >= 3.7); see RESEARCH_LOG.md (P2).

numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
geopandas==1.1.3
shapely==2.1.2
fiona==1.10.1
pyarrow==21.0.0
rasterio==1.5.0
rasterstats==0.21.0
libpysal==4.15.0
esda==2.10.0
matplotlib==3.10.6
pystac-client==0.9.0
